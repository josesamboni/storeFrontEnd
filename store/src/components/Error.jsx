// //import React from "react";
// import { Container } from 'react-bootstrap';

// const Error = ({ error }) => {
//   // Safeguard to handle cases where error or error.data might not exist
//   const errorMessage = error?.data?.message || "Unknown error message";
//   const errorType = error?.data?.error || "Unknown error type";

//   return (
//     <Container className="d-flex flex-column justify-content-center align-items-center" style={{ height: "80vh" }}>
//       <h3 className="text-danger mb-2">Error Type: {errorType}</h3>
//       <h4 className="text-danger">Error Message: {errorMessage}</h4>
//     </Container>
//   );
// };

// export default Error;
